Run the docs-maintainer subagent to process DIFF_CONTEXT.md.

This should be run before or after a commit. The maintainer will:
1. Update module docs to reflect current code state
2. Draft decision records for significant entries
3. Clear DIFF_CONTEXT.md after processing

Review any drafted decision records before committing them.
