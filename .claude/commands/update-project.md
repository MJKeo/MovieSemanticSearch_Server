Update docs/PROJECT.md with the changes I describe.

After making the update:

1. Scan docs/decisions/ for any decisions whose justification
   relied on the section that changed (e.g., if priorities were
   reordered, find decisions that referenced the old priority order)

2. List any decisions that may need revisiting given the new
   context. Format:
   - [NNN] — [title]: [why it might be affected]

3. Check docs/modules/ for any module docs that reference the
   changed section and flag those too

Do NOT modify any decision records or module docs. Only report
which ones may be affected. I will decide what to update.
