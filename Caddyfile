api.cinemind.dev {
    tls /etc/caddy/certs/origin.pem /etc/caddy/certs/origin.key
    reverse_proxy api:8000

    # log to stdout so docker compose logs caddy works
    log {
        output stdout
        format console
    }
}
